<project name="Client" basedir="." default="main">

    <property name="src.dir1"     value="src/cis5027/project/clients/lightapp"/>
    <property name="src.dir2"     value="src/cis5027/project/clients/helpers"/>
    <property name="build.dir"   value="build_light_client"/>
    <property name="classes.dir" value="${build.dir}/classes"/>
    <property name="jar.dir"     value="${build.dir}/jar"/>

    <property name="main-class"  value="cis5027.project.clients.lightapp.frames.apps.LightApp"/>


    <target name="clean">
        <delete dir="${build.dir}"/>
    </target>

    <target name="compile">
        <mkdir dir="${classes.dir}"/>
        <javac srcdir="${src.dir1}:${src.dir2}" destdir="${classes.dir}"/>
    </target>

    <target name="jar" depends="compile">
        <mkdir dir="${jar.dir}"/>
        <jar destfile="${jar.dir}/${ant.project.name}.jar" basedir="${classes.dir}">
            <manifest>
                <attribute name="Main-Class" value="${main-class}"/>
            </manifest>
        </jar>
    </target>

	<!--
	ant -Darg0="5000" -f build_client.xml
	 -->
    <target name="run" depends="jar">
        <java jar="${jar.dir}/${ant.project.name}.jar" fork="true">
    		<arg value="${arg0}"/>    
        </java>
    </target>
	
    <target name="clean-build" depends="clean,jar"/>

    <target name="main" depends="clean,run"/>

</project>
